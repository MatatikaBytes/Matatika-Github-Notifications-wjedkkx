version: 2

macros:
  - name: week_trunc
    arguments:
      - name: thedate
        type: date
        description: "The date value to be used to calculate the week number"

      - name: offset
        type: integer
        description: "The number of days to offset the week calculation. Default is 0 for Monday - Sunday, 1 Sunday - Saturday."